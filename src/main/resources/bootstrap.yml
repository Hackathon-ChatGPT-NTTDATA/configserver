server:
  port: 7000

spring:
  banner:
    location: classpath:banner/banner.txt
    
  application:
    name: configserver
  cloud:
    config:
      server:
      
        git:
          default-label: master
          uri: https://github.com/Hackathon-ChatGPT-NTTDATA/configserver
          force-pull: true
          search-paths: config-data   
      enabled: true   


                       
